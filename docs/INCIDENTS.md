# Incident Ledger — Project Kriya

Append-only. **The most valuable file in this repository.**

Every failure lab and every real failure gets a row. Ninety of these 237 days end with something
deliberately broken; this is where what you saw — *before you knew the cause* — gets written down.

## Why the "first symptom" column is the important one

Debugging is a mapping problem: from the symptom you can see to the cause you cannot. Nobody is born
knowing that `CrashLoopBackOff` usually means the process exited, not that Kubernetes is broken; or
that a p99 latency spike with flat CPU is usually a lock or a downstream call; or that a drift alert
that fires the morning after a deploy is usually a broken feature pipeline rather than a changed
world.

You learn that mapping by building it, one row at a time. In six months this table is also the most
convincing document you own: a list of failures you personally caused and diagnosed beats any
certificate in any interview.

## How to fill a row

Write the **first symptom** before you investigate. Not after. The whole value is in recording what
was actually visible at the start, including the wrong thing you thought it was.

| # | Date | Day | What I broke | First symptom I saw | What it actually was | The smallest fix | What I changed so it cannot happen silently again |
| - | ---- | --- | ------------ | ------------------- | -------------------- | ---------------- | ------------------------------------------------- |
| 1 | 2026-08-24 | 0 | Staged a fake secret with `git add -f .env`, defeating `.gitignore` (part 3.5) | **none — the command printed nothing at all and succeeded.** `git add -A` beforehand had correctly staged 0 `.env` files; one `-f` changed that with no warning and no confirmation | `.gitignore` is a filter against accidents, not a barrier. It only ever applies to *untracked* paths, and `--force` overrides it by design | `git rm --cached .env` (leaves the file on disk), then `rm .env` | Nothing yet — and that is the finding. The real controls are a server-side scanner (Day 17) and never having the file at all (Day 58). Rotation comes before cleanup if it is ever a live key |
| 2 | 2026-08-24 | 0 | Breakage 1 — an unused `import os` in `.probe/bad.py` | ``F401 [*] `os` imported but unused / --> .probe\bad.py:1:8`` | The gate stopped at check 1 of 5 and never ran the other four — `set -e` in `o` aborts on the first non-zero exit | `rm -rf .probe` | Nothing needed; the check already works. Confirmed exit=1, which is what CI reads |
| 3 | 2026-08-24 | 0 | Breakage 2 — badly formatted but valid Python, `x = { "a":1 }` | `All checks passed!` from the linter, then `unformatted: File would be reformatted / --> .probe\fmt.py:1:6` | Check 2 isolated from check 1: the linter has no opinion on this, the formatter does. **`diff` against a pre-run copy confirmed the file on disk was byte-identical** — `--check` reports and never rewrites | `rm -rf .probe` | Nothing needed. Verified the `--check` flag is what keeps this a check rather than a mutation |
| 4 | 2026-08-24 | 0 | Breakage 3 — a test that cannot pass, `assert 1 == 2` | `F [100%]` then `E       assert 1 == 2` and `FAILED tests/test_probe.py::test_deliberate` | **The first time the test step had ever run a test.** Every prior green came from pytest exiting `5` ("no tests collected"), which `\|\| [ $? -eq 5 ]` forgives. Confirmed the exemption does *not* swallow a genuine failure: gate exit=1 | `rm tests/test_probe.py`, after confirming pytest returned to exit 5 and the gate to exit 0 | Nothing needed — but noted that the exemption expires on Day 3, when real tests exist and an empty suite would become a silent regression |
| 5 | 2026-08-24 | 0 | Breakage 4 — appended `**Time:** about two hours` to `CHECKLIST.md` with `>>` | `FAIL day   0  1 problems` / `- CHECKLIST.md: a **Time:** line ('**Time:**') - a day has no clock (Principle 17)` / `depth contract: 0/1 days pass` | Principle 17 is enforced by `scripts/depth_check.py`, not merely documented. Lint and format both passed first — the clock is invisible to every check but this one | Restored the file from a copy taken before the append (the day folder was still untracked, so `git checkout --` was not available) | Took the backup *before* breaking a tracked-adjacent file. `>>` not `>` was the difference between an append and destroying 144 lines |
| 6 | 2026-08-24 | 0 | Breakage 5 — ran `./o done 0` with boxes unticked | `FAIL unticked boxes remain in days/day-000-toolchain-skeleton-driver/CHECKLIST.md`, followed by every offending line number (`23:`, `24:`, `25:` …) | The done gate refuses **before** running `./o check` at all — no lint, format or test output appeared. Cheapest check first | Ticked the boxes honestly | Nothing needed. Noted what it *cannot* verify: it counts `- [ ]` markers, so it detects an untidied box, never a dishonest tick |
| 7 | 2026-08-24 | 0 | Boundary hunt (documented) — the exact defect from row 2, placed in `days/day-000-*/lab/junk.py` | **nothing. `OK all green`, exit=0** | `extend-exclude = ["days/*/lab", "legacy"]` in `pyproject.toml`. Deliberate and documented: a lab is scratch space | `rm -rf lab/` | Nothing — this boundary is intentional. Recorded because an intentional gap still has to be *known* |
| 8 | 2026-08-24 | 0 | Boundary hunt (my own, 1) — put syntactically invalid Python, `def (((`, inside `lab/` | **nothing. exit=0** | The `lab/` exclusion is **total, not stylistic**. Control test: the identical file at `.probe/broken.py` gives `invalid-syntax: Expected an identifier` and ruff exit=1. So the gate will not tell me a lab file is unparseable — only running it will | `rm -rf lab/` | Nothing yet. Worth knowing before Day 3, when lab code starts being copied into `pulse/` — the moment it moves, the gate starts caring, and that transition is where a broken file surfaces |
| 9 | 2026-08-24 | 0 | Boundary hunt (my own, 2 — the important one) — appended `GROQ_API_KEY=gsk_LIVE_LOOKING_VALUE_123456` to `.env.example`, a **tracked** file, and ran the gate | **nothing. exit=0, all five checks green** | **The gate has no secret scanning whatsoever.** Today's entire secrets defence is `.gitignore`, which matches *paths* and never *content* — and `.env.example` is deliberately un-ignored. So the one file guaranteed to be committed is the one file nothing inspects. Rows 1 and 9 are the same hole from two directions: `-f` bypasses the path filter, and a tracked file never meets it | `git checkout -- .env.example` | Nothing yet — this is precisely what Day 17's CI secret scanner is for. Recording it now so that day closes a gap I found rather than one I was told about |
| 10 | 2026-08-24 | 0 | Pointed the **system** interpreter at the project's work instead of `.venv` (part 1.5) | `C:\Users\nisha_gnzw\AppData\Local\Programs\Python\Python312\python.exe: No module named pytest` — and the same for `ruff` | Exactly the symptom from part 1.1: nothing was deleted and nothing is broken. `python` and `uv run python` resolve to two different interpreters with two different site-packages shelves, and only one of them has the dev group installed | Use `uv run`, never bare `python` | Committed `.vscode/settings.json` with `python.defaultInterpreterPath` → `${workspaceFolder}/.venv/Scripts/python.exe` and `python.terminal.activateEnvironment: false`, so the editor's choice is in the repository instead of in one machine's memory |
